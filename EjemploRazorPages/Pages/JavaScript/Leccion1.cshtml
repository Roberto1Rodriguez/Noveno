@page
@model EjemploRazorPages.Pages.JavaScript.Leccion1Model
@{
}
<html>
    <head>
    <script>


    </script>
<title>Leccion 1</title>
    </head>
<body>
    <h1>Leccion 1</h1>
    <h2>Variables, tipos de datos, sintaxis</h2>


    <script>
        //scripts en head para metodos que se ejecutaran despues
        //script en body se ejcutan al inicio
        //tipos de datos: numeric, boolean, text, object, null(especial), undefined(especial, variable declarada que no ha tenido nada en su interior)
        //let sirve para guardar cualquier tipo de valor
        alert("hola");
        {
        var x=15;
        }
        let y="hola";
        y=false;
        let z={nombre:"juan",edad:"29"};
        console.log(x);
        var w= 5.3
        //declarar una variable var hacer que esta se declare desde antes mediante holsing
        //al usar let la variable solo se puede usar en su mismo bloque

        //const z;
 //       z = 5; //const tambien hace holsing

        //operadores aritmeticos=  +,-,*,/,%,**
        let a = 2 + 2;
        a = 3 ** 3;
        //logicos= &,|,!,&&,||
        //relaciones: >,<,>=,<=,==,===,!=
        //asignacion: +=, -=,*=,/=,&=,|=,++,--,=

        let b = 3.0;
        let c = 3;
        let d = b === c;

        // el operador === significa que dos valores son identicos en todos los aspectos, mientras que == solo le importa que tengan el mismo valor

        let e = true;
        //let f &=fasle;

        e |= true;

        //operadores de cadenas: +, ${}

        let s = "hola"

        let s2 = 'hola';

        let s4 = 'hola "juanito"';

        console.log(s4);

        let s5 = s2 + s4;
        //cuando uno de los operadores es cadena  todo se hace cadena

        //plantilla literal, (interpolacion)
        let num = 1;
        let num2 = 2;
        let res = `El valor es ${num} ${num2}`;
        let res2 = "El valor es ${num}";
        //solo se usa con comillas inversas

        console.log(res);
        console.log(res2);

        let caedna1 = "hola\
        a\
        todos";
        console.log(caedna1);
        //condicional: ?:

        let valor =1>2?5:3;

        if (1 > 2) { 
        }

        switch (valor) {
            case 1: valor++; break;
            default: valor--; break;
        }

        //ciclos
        while (valor > 10) {

        }

        do {

        }
        while (valor > 10) ;

        for (let i = 0; i < 10;i++) { 
        }


        let arreglo = [ 1, 2, 3, 4];


        //ciclo for-of

        for (let n of arreglo) {
            console.log(n);
        }


        for (let n in arreglo) {
            console.log(n);
        }
       
        //array

        let arr = [];
        arr[0] = 5;
        arr[1] = "hola";
        arr[10] = true;

        console.log(arr);
        console.log(arr.length);

        arr.push(100);// agrega o quita del inicio de la fila
        arr.shift(200);//agrega o quita del final de la fila

        let cad1="100";
        let numero = parseInt( cad1) + 10;
        console.log(numero);




    </script>
</body>
</html>
